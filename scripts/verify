#!/usr/bin/env bash
set -euo pipefail
repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"
echo "=== NonsulFit Verify ==="
./scripts/agent/doctor
./scripts/lint
./scripts/typecheck
./scripts/test
echo "=== Verify Complete (contract lint, typecheck, and tests passed) ==="
